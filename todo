- Тип ноды надо сделать свойством ноды. Иначе трансформаторы не могут выставлять тип. Тип должен генерироватся чеез интерфейс
- Тип линка надо сделать свойством линка. Тип должен генерироватся чеез интерфейс
- Сейчас фильтрация делается пл id. Это может быть не очень удобно, так как хочется фильтровать по полному имени символа без указания сборки
- Добавить в json экспорт модификатор видимости и статик флаг
- У пространства имен должны быть ссылки только если используются имено они а ни их подпространства
- Кажется нет никакого смысла создавать ноды для геннерируемого кода, так как граф формируется для кода который создал юзер.
    Но есть исключение когда сгененрированый код находится в видимом расположении.

- Похоже что есть проблемы с выражениями которые находтся в инициализаторах свойств
    var q = new{
        count = t.Count() -< проблема с сылокой на Count
    }

- Нужно создать сборку с интеграционными тестами. В которой создать решение с генератарами и несколькоми targetframework

- Проверить что проект с targetframeworks не дублирует ссылки

- ??? Похоже проекты с targetframeworks на каждый фреймворк создаеют свой символ. Сейчас у ноды можно указать только
    один символ. Надо подумать нужно ли у ноды делать много символов

- ??? В теории может быть код который через ifdef может менять список модификаторов у символа.
    Например public ifdef net8 struct elseif class endif TestClass. Поэтому надо добавлять в id
    идентификаторы символа. Возможно это не играет роли так мы получим один id на всех и когда будет
    создаватся ссылка она должна искать символ по id для связи с узлом



